<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Epilogue:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
        }
        body {
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: linear-gradient(#60d3e4, #5fd4ad);
            color: rgb(255, 255, 255);
            font-family: 'Epilogue', sans-serif;
        }
        .top-bar {
            background: #5fd4ad;
            padding: 20px 10%;
            border-bottom: 3px solid #5fd4ad;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .top-bar h1 {
            margin: 0;
        }
        .logo {
            width: 50px; /* Adjust the width as needed */
            height: auto;
            margin-right: 10px; /* Space between the logo and title */
        }
        .header-right {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        .header-right a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s;
        }
        .header-right a:hover {
            color: #ffffff;
        }
        .dashboard-container {
            display: flex;
            flex: 1;
        }
        .main-content {
            flex: 1;
            padding: 20px;
            margin-left: 250px;
            transition: margin-left 0.3s ease;
        }
        .section {
            background: #1a1a1a;
            padding: 20px;
            margin-top: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
            border: 2px solid #733b61;
        }
        .content {
            display: flex;
            gap: 20px;
        }
        .left-section, .right-section {
            flex: 5;
        }
        .block {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 15px 30px;
            margin-top: 20px;
            border-radius: 20px;
            color: rgb(0, 0, 0);
            text-align: center;
            cursor: pointer;
            border: 2px solid rgb(255, 255, 255); /* White border */
            transition: background-color 0.3s, border-color 0.3s;
        }
        .block:hover {
            background-color: #ffffff;
            border-color: #ffffff;
        }
        .block:active {
            background-color: #8b6176;
            border-color: #b90059;
        }
        .logout-button {
            background-color: #05847d;
            color: rgb(255, 255, 255);
            padding: 5px 10px;
            text-decoration: none;
            border-radius: 5px;
        }
        .logout-button:hover {
            background: color #222;
            border: 1px solid #231c20;
        }
        .main-content.closed {
            margin-left: 0;
        }
    </style>
</head>
<body>
    <header class="top-bar">
        <img src="your-logo-path.png" alt="Logo" class="logo"> <!-- Replace "your-logo-path.png" with your logo image path -->
        <div class="header-right">
            <span>Welcome, <%- username %>!</span>
            <a href="/calendar">Calendar</a>
            <a href="Stats">Stats</a>
            <a href="/wallet">Wallet</a>
            <a href="About us">About us</a>
            <a href="/logout" class="logout-button">Logout</a>
        </div>
    </header>

       
        <main class="main-content">
            <div class="content">
                <div class="left-section">
                    <!-- Wallet block with link to the wallet page -->
                    <a href="/wallet" class="block">Wallet <br> 999,999,999.99</a>
                    <div class="block">Transaction</div>
                    <div class="block">Summary</div>
                </div>
                <div class="right-section">
                    <!-- Button-like block for categories -->
                    <div class="block">Categories</div>
                </div>
            </div>
        </main>
    </div>

    <script>
        // Function to toggle the sidebar
        function toggleSidebar() {
            const sidebar = document.querySelector('.sidebar');
            const mainContent = document.querySelector('.main-content');
            sidebar.classList.toggle('closed');
            mainContent.classList.toggle('closed');
        }
    </script>
</body>
</html>
